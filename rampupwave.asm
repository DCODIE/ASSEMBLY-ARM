;INTERFACE A DAC TO GENERATE A RAMP WAVE FORM(UP)

.MODEL SMALL
.STACK  20
.DATA
.CODE
START:  MOV AX,@DATA
	MOV DS,AX
	MOV AL,80H
MOV DX,24a3H
OUT DX,AL
L1:     MOV AL,00H
UP:     MOV DX,24a0H
	OUT DX,AL
	INC AL
	CMP AL,0FFH
	JNE UP
	JMP L1
       ; INT 03H
END START
