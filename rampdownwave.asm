;INTERFACE A DAC TO GENERATE A RAMP WAVE FORM(DOWN)

.MODEL SMALL
.STACK  100
.DATA
.CODE
START:  MOV AX,@DATA
	MOV DS,AX
	MOV AL,80H
MOV DX,24a3H
OUT DX,AL
L1:     MOV AL,0FFH
UP:     MOV DX,24a0H
	OUT DX,AL
	DEC AL
	CMP AL,00H
	JNE UP
	JMP L1
	INT 03H
END START
